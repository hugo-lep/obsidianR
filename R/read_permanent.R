#' Lire les notes permanentes (concepts)
#'
#' Lit toutes les notes de type `note permanente` du vault.
#'
#' @param vault_path Chemin vers le dossier vault. Si NULL, lu depuis config.yml.
#'
#' @return Un tibble avec une ligne par note : `title`, `tags`, `moc`, `body`, `path`.
#' @export
read_permanent <- function(vault_path = NULL) {

}
